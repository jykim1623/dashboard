const gAxes = () => {};

const gXaxis = () => {};

const gYaxis = () => {};

const gXscale = ({ type, field }) => {
  const result = {
    name: "xscale",
  };
};

const gYscale = () => {};

const gScale = () => {};

export const generatorSpec = (opt) => {};
